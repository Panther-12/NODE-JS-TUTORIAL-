// Node is a framework used to run javascript code outside the browser
// Simple javascript code can be run using the node REPL(Read Evaluate Print Loop)
// command "node app/node app.js"


// sample code that cannot be run inside the REPL therefore run inside
// the  node CLI (Command Line Interface)
const exams = 3;
if (exams < 10){
    console.log("managable");
}
else{
    console.log("unmanagable");

}
console.log('hello nimrod your first app');