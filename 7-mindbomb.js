// declare two variables
var num1 = 1;
var num2 = 2;

const sumation = ()=>{
    // the quotes are used to allow calculation within a string
    console.log(`sum of the two numbers is: ${num1,num2}`);
}

sumation();


module.exports = sumation ;